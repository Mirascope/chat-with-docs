`11:38` What is the right way to pass temperature and other model config params to each of these calls:

```   # Call with OpenAI
    openai_call = openai.call("gpt-4o")(prompt_fn)
    openai_response = await openai_call(product_name=product_name)

    # Call with Anthropic
    anthropic_call = anthropic.call("claude-3")(prompt_fn)
    anthropic_response = await anthropic_call(product_name=product_name)```