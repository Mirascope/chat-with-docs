`00:22` I’ve recently deployed some stuff into prod - `from mirascope.core import litellm, prompt_template`

with `@litellm.call("bedrock/anthropic.claude-3-5-sonnet-20240620-v1:0")`

And it’s complaining about no OPENAI_API_KEY, for now I have created a dummy one. Is this expected behaviour?