`05:01` Quick question, when writing docstring for the tool, can the argument docstring be splitted with new lines?
Here is an example:
```    Args:
        design_option: The design option to query (e.g., "necklineStyle", "sleeveLength", "bodyFit").
        sales_metric: Sales metric to sort by. Can be "UnitsSoldFullPrice", "RevenueFullPrice", "UnitsSoldDiscounted", "RevenueDiscounted", "TotalUnitsSold", or "TotalRevenue". 
        Default is "TotalUnitsSold".
        asc: Whether to sort the results in ascending order. Default is False (descending order).```
Notice the phrase "Default is ..."
it's on the new line so I'm wondering whether it's going to be extracted properly into the LLM as `sales_metric` hint.