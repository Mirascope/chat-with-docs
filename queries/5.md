`20:59` What's the correct way to pass dynamically-generated schemas at runtime? I used to do a lot of Pydantic generations like this:

```class MyResult(BaseModel):
    @staticmethod
    def create_dynamic_schema(things: list[dict]) -> Type["MyResult"]:
        fields = {}
        for thing in things:
            if thing["type"] == "a":
                fields[thing["label"]] = (
                    bool,
                    Field(
                        description=thing["requirement"],
                    ),
                )

        return create_model(
            "MyResult", **fields, __base__=MyResult
        )

# ...

schema = MyResult.create_dynamic_schema(...)
dynamic_result = ExtractionPrompt(extract_schema=schema).extract()```
Wondering if I should just bypass the Pydantic model entirely, but would love some advice on this piece