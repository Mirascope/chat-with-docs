Hi, what's the best way to reuse a multiline prompt with many methods using Mirascope?

While exploring this I also ran into some type inference issues, this causes an error:
   messages = [Messages.User(f"Recommend a {genre} book")]
   return {
        "messages": messages,
        "client": AsyncOpenAI(),
    }

While this (from the example) does not:
   return {
        "messages": [Messages.User(f"Recommend a {genre} book")],
        "client": AsyncOpenAI(),
    }

There might be a better way of reusing prompt, so the above might be a non-issue.