Hey guys, what is the right way to use azure openai?
Currently i have this:

```python
client = AzureOpenAI(
    api_key=os.getenv("AZURE_OPENAI_API_KEY"),
    api_version="2024-07-01-preview",
    azure_endpoint=os.getenv("AZURE_OPENAI_ENDPOINT")
)

@llm.call(model="gpt-4o-mini", provider="azure", response_model=ExtractionResult, client=client, json_mode=True)
def extract_problem(prompt: str, case: Type[Case] | None, activities: str) -> ExtractionResult:
    # Build the prompt by replacing placeholders with actual case values.
    prompt = prompt.format(
        case_subject=case.subject, activities=activities
    )
    return prompt
```

But unfortunatly, I get:

```
AttributeError: 'AzureOpenAI' object has no attribute 'complete'
Traceback:
File "/Users/tomas/code/src/pages/06_Problem_Solution_Extractor.py", line 152, in <module>
    extracted_info = extract_problem(user_prompt_template, case_obj, activities_text)
File "/Users/tomas/code/.venv/lib/python3.13/site-packages/mirascope/llm/_call.py", line 346, in inner
    result = decorated(*args, **kwargs)
File "/Users/tomas/code/.venv/lib/python3.13/site-packages/mirascope/core/base/_extract.py", line 158, in inner
    call_response = create_decorator(fn=fn, **create_decorator_kwargs)(
        *args, **kwargs
    )
File "/Users/tomas/code/.venv/lib/python3.13/site-packages/mirascope/core/base/_create.py", line 219, in inner
    create, prompt_template, messages, tool_types, call_kwargs = setup_call(  # pyright: ignore [reportCallIssue]
                                                                 ~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
        model=model,
        ^^^^^^^^^^^^
    ...<8 lines>...
        stream=False,
        ^^^^^^^^^^^^^
    )
    ^
File "/Users/tomas/code/up-master-thesis/.venv/lib/python3.13/site-packages/mirascope/core/azure/_utils/_setup_call.py", line 179, in setup_call
    client.complete,
    ^^^^^^^^^^^^^^^
```
Any help/hint is much appreciated. I have extensively searched through the docs