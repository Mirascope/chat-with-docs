`08:34` Hey everyone, I've been playing around with the structured streaming and I've been getting quite flaky behaviour

```# WORKS (most of the time) without streaming
@openai.call(
    model="gpt-4o-mini",
    response_model=Test,
    json_mode=True,
    stream=False,
)
@prompt_template("Write a poem and rate it out of 10")
def agent1(): ...


print(agent1())


# DOESNT WORK (most of the time) with streaming
@openai.call(
    model="gpt-4o-mini",
    response_model=Test,
    json_mode=True,
    stream=True,
)
@prompt_template("Write a poem and rate it out of 10")
def agent2(): ...


for x in agent2():
    print(x)```
Couple observations:
• it seems like using JSON mode on True makes the streaming call always fail (with `jiter.from_json(ValueError: EOF while parsing a value at line 1 column 0` )
• non-streaming seems to be working better than streaming but still fails occasionally
• I am surprised there appears to be such a difference between streaming and non-streaming since the token output should be (relatively) the same, I suppose there is probably something going on with how partial JSONs are handled
Is there something that I am doing obviously wrong here?

This is a small example but I'm working with a case that's actually more complex. In my real case it always works with `streaming=False` but I always get that ``EOF while parsing a value at line 1 column 0` whenever I set `streaming=True`

Thanks!

BTW it's great to see mirascope propose a different approach, LangChain almost had me throw my laptop through the window a few times already