`12:33` Is chaining the best way to "fix" output from a model that struggles to produce JSON consistently? I.e. take the model output and run it through another fast/cheap model to make it conform to my schema? I'm running into an issue where a model i want to use doesn't support tool/function calls or json/md mode.

For example if a PDF extracting model like paligemma can't reliably output JSON i can still provide a JSON schema of the data I want extracted to mirascope and have it convert it by using mirascope extraction.

Paligemma output:
```EXTRACTED DOCUMENT ELEMENTS
Person: Jim Johnson
Title: Software Engineer
Salary: $155,000
Location: Chicago, IL ```
Send to gemini flash to get:
```{
  "person": "Jim Johnson",
  "title": "Software Engineer",
  "salary": 155000,
  "location": "Chicago, IL"
}```