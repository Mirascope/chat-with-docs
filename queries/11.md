Another quick question, or more feature, is it possible to make the model automatically try again to generate a json struct when it fails to do so the first type (get an error) and tell him by passing the not well formed json it has made an error ? I coud try to implement that, is that something that would interest you rn ?