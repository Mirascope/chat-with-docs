`23:41` I'm still confused about https://mirascope.com/docs/learn/tools/. The point of tools is that the LLM can request that the tool be called so that the results of the tool call can be sent in a new request back to the LLM, appended to the original messages that resulted in the LLM requesting a tool call.  All of the examples seem to only print out the result of calling the tool like: "`print(tool.call())`".

Am I understanding this right?  I'm supposed to actually call the tool?  When does the tool_call result get sent back to the LLM (completions api)? Do I need to do that or has that already occurred?