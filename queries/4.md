We can use semaphore to control # of concurrently running parallel asyncio tasks. Given you can likewise run multiple mirascope llm calls in parallel as indicated in docs, how would we use a semaphore to limit the concurrency? Can we just add the "with semaphore" guard inside the call function?

Or is there something more convenient (hint, hint) in mirascope already for that? 