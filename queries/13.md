`06:06` Hi folks, what is the recommended way to debug the rendered prompt when using response_model for extraction?

I am trying to debug the call made to the LLM. I would like to see the prompt *after* rendering. I am using the response_model feature, and in that case I cannot figure out how to access the rendered prompt.

`@openai_call(response_model=MyModel, json_mode=True)`
    @prompt_template(template)
    def run(
        self,
        my_param: str,
    ):
        """Extract a MyModel from my_param."""

I cannot use online visibility platforms like logfire. I have tried using open telemetry and I get some logs dumped to the console, but unlike in the example in the doc it does not seem to give the rendered prompt, only the prompt template.