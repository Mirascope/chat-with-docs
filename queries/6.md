hey all, i’m trying to use mirascope with streaming tool calls. i’m struggling to get a stream (sync or async) of the tool call response from gpt-4o. i end up just getting a full dump of the full tool call response rather than chunk-by-chunk on the JSON being returned by gpt-4o, so is effectively the same as a non-stream request. (i’ve used tool calls a lot so understand their purpose within LLM-driven features)

i’ve had a look around the docs and searched in here for examples but can’t find anything that clearly demonstrates how to do a chunk-by-chunk stream of a tool call response. i think i’m missing something that sits between the llm decorated call and the tool class i’ve defined. can someone provide or point me in the direction of an example for how to implement this sort of streaming? is it even possible in mirascope?