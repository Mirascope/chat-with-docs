We primarily use mirascope for text classification and text extraction.
```python
            @llm.call(
                provider="openai",
                model=self.settings.AZURE_OPENAI_MODEL_NAME,
                response_model=ESIAnalysis,
                client=self.client,
                max_tokens=100,
            )
            @prompt_template(
                """
```
is there any option to change max_tokens? I tried from decorators but it didn't work