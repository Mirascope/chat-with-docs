`06:39` Hello guys , getting issue with gemini usage . It was working with anthropic and OpenAI , but decided to use gemini and got stucked .

Getting this error : `Unhandled exception occurred: Unfortunately Google's Gemini API cannot handle nested structures with $defs.`

To reproduce :

```from typing import Dict, List

from mirascope.core import gemini, prompt_template
from pydantic import BaseModel


class DayPlan(BaseModel):
    day: int
    title: str
    description: str


class JourneyRoadmap(BaseModel):
    title: str
    days: List[DayPlan] 


@gemini.call("gemini-1.5-pro", response_model=JourneyRoadmap, json_mode=True)
@prompt_template(
    """Generate a 30-day language learning journey for {focus_area}. 
    Provide a roadmap with a title and description for each day. 
    Format the output as a JSON object with the following structure:
    {{
        "title": "30-Day {focus_area} Journey",
        "days": [
            {{
                "day": 1,
                "title": "Day 1 Title",
                "description": "Brief description of day 1 focus"
            }},
            ...
        ]
    }}
    """
)
def generate_journey_roadmap(focus_area: str) -> JourneyRoadmap: ...```
Is something different with gemini usage ? `@William`  Hey :wave: